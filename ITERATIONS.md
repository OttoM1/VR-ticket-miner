# Future Iterations & Features

### Hour band & parsing window

- The hour band should become adjustable from null to 4 hours.
- More origin & destination options to builder.
- Direct route only filtering.
- Builder landing hero

chmod +x bash/mytest.sh
./bash/mytest.sh
which bash
/bin/bash

---

- which bash
- whereis bash
- echo $SHELL
  command -v bash

  --- old env.sh

  #!/usr/bin/env bash

# compatibility env check

set -uo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)";

if ! command -v node >/dev/null; then
echo "Install Node 20+ first"
else echo "Node 20+ active"
if ! command -v git >/dev/null; then
echo "Install git"
else echo "Git running"
fi;

exec $SHELL;

fi;
